# Ask the user for a number and print its factorial using math.factorial().
import math

num = int(input("Enter the no. to calculate factorial = "))
print(f"Factorial of {num} = {math.factorial(num)}")